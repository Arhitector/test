import styled from 'styled-components';

const Elevators  = styled.div`
    position: relative;
    z-index: 10;
    width: 100%;
    display: flex;
    justify-content: space-around;
`;

export default {
    Elevators
};